{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["tsconfig.json"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tsconfig.json", "tsdown.config.ts"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**", "tsconfig.json"],
      "outputs": ["*.tsbuildinfo"]
    },
    "test": {
      "inputs": ["src/**", "test/**", "tsconfig.json", "vitest.config.ts"]
    },
    "test:stress": {
      "cache": false,
      "inputs": ["src/**", "test/**", "scripts/**", "tsconfig.json"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "prepublishOnly": {
      "dependsOn": ["build"],
      "outputs": ["dist/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
